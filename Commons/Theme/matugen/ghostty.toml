[config]

[templates.ghostty]
input_path = './templates/ghostty'
output_path = '~/.config/ghostty/lyingshell-colors'
# ghostty reloads its config on SIGUSR2 (no remote control needed).
post_hook = 'pkill -USR2 -x ghostty 2>/dev/null || pkill -USR2 -x .ghostty-wrappe 2>/dev/null || true'
