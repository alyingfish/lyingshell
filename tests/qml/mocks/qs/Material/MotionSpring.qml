import "../../../../../Material" as Real

// Forward the production spring through the explicit test qmldir. Product
// imports are discovered by Quickshell; plain qml6 needs this module entry.
Real.MotionSpring {}
